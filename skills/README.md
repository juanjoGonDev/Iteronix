# Skills

This directory defines reusable skills the agent can execute.

A skill is a repeatable capability with:

- a clear purpose
- strict input/output expectations
- execution rules
- acceptance criteria

Skills are referenced implicitly by the agent when relevant.
They must be followed strictly.

If a task matches a skill definition, the agent MUST apply it instead of improvising.
